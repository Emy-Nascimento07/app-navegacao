import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Card = () => {
  return (
    <View style={styles.card}>
      <Text>Início do card</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  card: {
    backgroundColor: #e85c5c,
  },
});
